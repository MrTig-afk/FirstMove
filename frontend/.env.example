# FirstMove frontend — copy to frontend/.env.local. Vite exposes VITE_* to the
# browser bundle, so nothing here is secret (the API key is rate-limit gating,
# not auth — it ships to every client).
VITE_API_URL=http://localhost:8000
VITE_API_KEY=dev-key
